---
status: active
branch: claude/optimize-codex-marketplace-uVtJK
capabilities:
  new: []
  modified: [multi-target-distribution, release-workflow]
  removed: []
---

## Why

Commit `71c000fc` shipped `.agents/plugins/marketplace.json` with a Git-URL source, fixing the Codex install path that PR #46's auto-discovery assumption had left broken (Issue #51). The catalog file works — but the spec, AGENTS.md, README, and CONSTITUTION still claim *"no separate Codex marketplace catalog file is shipped"* and the README documents the wrong install command (`codex /plugins`). This change closes the contradiction so the documentation describes shipped reality.

## What Changes

- Spec `multi-target-distribution.md` (v4 → v5): flip Requirement "Codex Discovery via Marketplace Add" from forbidding to mandating `.agents/plugins/marketplace.json`; replace the "No catalog file shipped" Scenario with a "Catalog file shipped" Scenario; rewrite the Edge Case + Assumption that depended on the falsified auto-discovery model. Add a new Requirement "Codex Marketplace Catalog Schema" documenting the Git-URL source form.
- Spec `release-workflow.md` (v5 → v6): rewrite the "no separate Codex marketplace catalog file is shipped" sentence in Requirement "Source and Release Directory Structure" — now four hand-edited root files, three version-stamped + one shape-stable.
- `AGENTS.md` File Ownership block: replace the same wording.
- `.specshift/CONSTITUTION.md` Architecture Rules: replace the same wording.
- `README.md`: replace `codex /plugins` install snippet with the canonical two-step `codex plugin marketplace add github:fritze-dev/specshift` + `codex plugin install specshift` flow plus an Update subsection. Add `.agents/plugins/marketplace.json` to the Multi-Target Distribution tree diagram.
- `docs/decisions/adr-003-shopify-flat-multi-target-distribution.md`: small Decision 6 amendment recording the falsification of the auto-discovery assumption and the choice of Git-URL over `local`-path source.
- Auto-generated by `specshift finalize`: `CHANGELOG.md` v0.2.6-beta entry, `src/VERSION` patch bump, `docs/capabilities/multi-target-distribution.md` regenerated.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `multi-target-distribution`: Codex Discovery via Marketplace Add (clause inversion + scenario replacement); new Codex Marketplace Catalog Schema requirement; Edge Case + Assumption rewrites for the falsified auto-discovery model.
- `release-workflow`: Source and Release Directory Structure — adjusted file count and wording around the Codex catalog.

### Removed Capabilities

None.

### Consolidation Check

1. Existing specs reviewed: `docs/specs/multi-target-distribution.md` (v4), `docs/specs/release-workflow.md` (v5). Both already cover Codex distribution and version stamping — they are the natural homes for the catalog clause and the file-count wording.
2. Overlap assessment: no new capabilities proposed; both edits fit within sister requirements that already exist in those two specs.
3. Merge assessment: N/A — modifications only.

## Impact

- Authoritative texts (specs, AGENTS.md, CONSTITUTION, README) align with shipped reality at `.agents/plugins/marketplace.json`.
- Codex consumers see the canonical install path in README and ADR-003 records the schema choice for future maintainers.
- No code behavior change. `bash scripts/compile-skills.sh` runs unchanged (still three version-stamping passes; the new catalog file is ignored by the script in this change).
- Version bump `0.2.5-beta → 0.2.6-beta` propagates to `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.codex-plugin/plugin.json` via the existing `jq`-stamper; the new catalog file has no `version` field and is left untouched.

## Scope & Boundaries

**In scope:**
- Spec text updates (`multi-target-distribution.md`, `release-workflow.md`) including frontmatter version bumps.
- AGENTS.md, CONSTITUTION.md, README.md text updates.
- ADR-003 Decision 6 amendment.
- CHANGELOG entry, src/VERSION bump, capability-doc regeneration (auto via `specshift finalize`).

**Out of scope (Non-Goals):**
- Converting Claude marketplace to Git URL — `"./"` stays canonical.
- Adding `verify_catalog_shape()` to `scripts/compile-skills.sh` — defer to follow-up issue.
- Extending `.github/workflows/release.yml` cross-check loop — defer to follow-up issue.
- Generating a sub-payload under `plugins/specshift/` (PR #53 pattern — schema-bound to `local` source).
- Closing PR #52, PR #53, Issue #51 — research-only inputs in this change.
- Live Codex smoke test on a clean install — separate verification.
- Auto-creating a draft PR during propose — session policy (no PR without explicit user ask).
